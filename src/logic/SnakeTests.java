/*import logic.Level;
import org.junit.Test;

import static org.junit.Assert.*;

public class SnakeTests {

    @Test
    public void testPositionAfterMoving() throws Exception {
        logic.Game game = new logic.Game(new logic.Level(4));
        SnakeDirection dir = SnakeDirection.Up;
        logic.Point currentSnakePosition = new logic.Point(game.getSnakeHead().column, game.getSnakeHead().row);
        //SnakeDirection currentSnakeDir = game.get

        //assertEquals(new logic.Point(currentSnakePosition.X, currentSnakePosition.Y));

    }

}*/